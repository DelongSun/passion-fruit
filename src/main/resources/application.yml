spring:
  application:
    name: passion-fruit
  profiles:
    active: default

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/passion_fruit?useSSL=false&useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&autoReconnect=true&zeroDateTimeBehavior=CONVERT_TO_NULL
    username: root
    password: root
    hikari:
      minimum-idle: 5
      maximum-pool-size: 15
      auto-commit: true
      idle-timeout: 30000
      pool-name: DatebookHikariCP
      max-lifetime: 120000
      connection-timeout: 30000
      connection-test-query: SELECT 1

  redis:
    host: 39.105.210.213
    port: 6379
    database: 4
    password: 'WSzs*aA^U2nH$u!K'
    lettuce:
      cluster:
        refresh:
          adaptive: true
          period: 30000
      pool:
        max-wait: 10000
        max-idle: 10
        min-idle: 10
        max-active: 20
  web:
    locale: zh_CN

server:
  port: 10066
  address: 0.0.0.0

logging:
  level:
    org.springframework.jdbc.core: DEBUG
    com.tooldin.passion: DEBUG